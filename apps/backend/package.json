{
  "name": "@repo/backend",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint src --ext ts --max-warnings 0",
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^1.0.0"
  }
}